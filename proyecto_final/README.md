# proyecto final

* in this folder run:

 `npm i`
* then:

 `npm run all-install`
* then:

 `npm run dev`
* then:

in the server folder add `.env` file
and add the next lines:

``` 
API_KEY=your_rapidapi_key
```
