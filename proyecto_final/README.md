# proyecto final

- in this folder run:

`npm run all-install`

- next:

`npm run dev`

- next:

in the client folder add `.env.local` file
and add the next lines:
```
REACT_APP_API_KEY=your_rapidapi_key
```